GET
    Implement advanced search features
        >  fieldname.gt = val
        <   .lt
        >=  .gte
        <=  .lte
        <>  .ne

        LIKE .like

    Implement Sort Options
        asc  _sortBy = fieldname.asc
        desc _sortBy = fieldname.desc
        multiple fields

    Implement Pagination
        _limit = int
        _offset = int

    Allow for selection of specific fields
        _fields = 

Request Rate Limiting